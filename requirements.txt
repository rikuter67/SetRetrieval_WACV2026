# -- [Frameworks] Install in a specific order if needed, but pip should handle it
# Pinned to versions compatible with CUDA 11.8 and each other
tensorflow==2.13.1

# PyTorch for CUDA 11.8, specified via index URL
torch==2.1.2 --index-url https://download.pytorch.org/whl/cu118
torchvision==0.16.2 --index-url https://download.pytorch.org/whl/cu118

# -- [HuggingFace Ecosystem] ---
transformers
tokenizers
huggingface-hub
safetensors
openai-clip

# -- [Core Scientific & Utility Libraries] ---
numpy<1.25
pandas
scikit-learn
matplotlib
Pillow
tqdm
seaborn
h5py

# -- [TensorFlow Dependencies] Pinning to compatible versions ---
keras==2.13.1
tensorboard==2.13.0
tensorflow-estimator==2.13.0
tensorflow-io-gcs-filesystem
absl-py
astunparse
gast<=0.4.0
google-pasta
grpcio
libclang
opt-einsum
protobuf<4.21.0
termcolor
wrapt

# --- [Compatibility Fix] Crucial for resolving conflicts ---
typing-extensions<4.6.0